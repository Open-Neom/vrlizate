name: vrlizate_example
description: Minimal VRlizate example app.
publish_to: 'none'
version: 1.1.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  vrlizate:
    path: ../
  vector_math: ^2.2.0
  sensors_plus: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
